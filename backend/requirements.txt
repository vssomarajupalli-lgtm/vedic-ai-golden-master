pytest==8.2.2
fastapi==0.111.0
uvicorn==0.30.1
pydantic-settings==2.3.4
jinja2==3.1.4
weasyprint==62.1
pydyf<0.11.0

# Optional AI providers (NOT required for import — MockProvider is the default).
# Install only when using a real OpenAI/Anthropic provider for the AI explanation layer:
#   openai>=1.0.0
#   anthropic>=0.26.0